listrik_db adalah recovery dari table yang ada di database listrik







#cek koneksi database

if ($conn->connect_error) {
    die("❌ Koneksi gagal: " . $conn->connect_error);
} else {
    echo "✅ Koneksi ke database berhasil!";
}

$conn->close();

http://localhost/pembayaran-listrik/config/database.php